// import { WebSocketProvider } from "../src/websocket-provider";


describe("Test Websocket Provider", () => {
    test("tesseracts", () => {
        // try {
        //     const provider = new WebSocketProvider("ws://localhost:1600/ws", {reconnectOptions: {
        //         auto: true,
        //         delay: 5000
        //     }});

        //     provider.on("connect", () => {
        //         console.log("Connection established successfully")
        //     })

        //     provider.on("reconnect", (val) => {
        //         console.log("Reconnection successfull", val)
        //     })

        //     provider.on("error", (event) => {
        //         console.log("Failed to establish connection", event)
        //     })

        //     provider.on("close", () => {
        //         console.log("Connection closed abnormally")
        //     })

        //     provider.on("all_tesseracts", (result) => {
        //         console.log(result);
        //     });
            
        // } catch(err) {
        //     console.log(err)
        // }

        // return new Promise((resolve, reject) => {
        //     setTimeout(() => {
        //         resolve(0)
        //     }, 100 * 1000)
        // })
    });
});
